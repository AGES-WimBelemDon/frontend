import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      light: '#387b81',
      main: '#075b62',
      dark: '#043f44',
    },
    secondary: {
      light: '#a3d266',
      main: '#8dc740',
      dark: '#628b2c',
    },
  },
});
