export type ApiClass = {
  id: number;
  name: string;
  weekDay: string;
  schedule: string;
  level: string;
  activityId: string;
  teacher: string;
};

export type StudentFrequency = {
  id: number;
  name: string;
  frequency: number;
}
