export type ApiClass = {
  id: number;
  title: string;
  weekDay: string;
  schedule: string;
  level: string;
  activityId: string;
  teacher: string;
};

export type StudentFrequency = {
  id: number;
  name: string;
  frequency: number;
}
